# Copie este arquivo para ".env" e preencha com os valores reais.
# O .env é ignorado pelo git (não comite credenciais).

# URL base da aplicação sob teste.
# Sobrescreve o baseURL do playwright.config.ts quando definido.
BASE_URL=https://gestao-frota-teste-qa.lovable.app

# Credenciais de login (caso a aplicação exija autenticação).
# APP_USERNAME=
# APP_PASSWORD=
